# entities.py
class agent:

    def __init__(id, personality):
        id = id
        personality = personality
        memory = memory[]

    def prime(initial_perspective: perspective, current_turn: turn) -> primer:

    def intent(current_turn: turn) -> intention:

    def add_memory(perspective):

class environment:

    def __init__(environment):
        entity_id = environment.id
        primer = environment.primer
        reality = reality[0 = environment.initial_reality]

    def initial_reflection(agent_id: entity_id, agent_personality: personality, turn_current: turn) -> initial_perspective:

    def reflect(agent_id, personality, turn) -> perspective:

    def add_intention(agent_id, intention, turn):

    def divine():

